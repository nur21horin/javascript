function add(num1, num2) {
  const result = num1 + num2;
  console.log(num1, num2, result);
  return result;
}

add(4, 5);

function fullname(first, last) {
  const full = first + " " + last;
  console.log(full);
  return full;
}
fullname("nur", "mohammod");


